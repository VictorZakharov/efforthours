# Public synthetic Change source provenance

Suite: `efforthours-change-public-synthetic/0.1.0`

All source states in `0.1.0.fixtures.json` were authored specifically for EffortHours
on August 6, 2026. They are project-owned synthetic examples, not copies of client,
employer, benchmark-repository, or third-party source. They are distributed under
the repository's root MIT license.

The suite contains no credentials, personal data, contributor identities, actual
labor records, commit timestamps, or historical activity. Its revision-like
selectors and object IDs are synthetic content identities used only to reproduce
final-delta selection and reconciliation behavior.

Generated artifacts under `0.1.0/` use:

- generator `change-fixture-generator/0.1.0`;
- Change estimator `change-seed/0.1.0+seed-rules/0.2.1`;
- profile `implementation` without pricing; and
- rubric `change-ehe-work-item/1.0.0` for blind authoring packets.

The exact suite digest and every report/final-delta digest are recorded in
`0.1.0/index.json`. Generated candidate reports are reproducible source
measurements, not reviewed labels. Blind authoring packets contain no candidate
total, category totals, work-item hours, or confidence values.

The separate `0.1.0.teacher-policy.json` is a project-authored weak-supervision
artifact mapped by `change-teacher-plan-generator/0.1.0`. Its review provenance
discloses that candidate totals were visible during invariant verification. The
compiled teacher corpus remains MIT-licensed project data and has no actual-labor
or independently validated ground-truth claim.
